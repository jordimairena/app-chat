<div class="row chat">
  <% @chats.each do |chat| %>
    <% if chat.sender == current_user %>
      <blockquote>
        <p><%= chat.mensaje %></p>
        <small>Sent to <cite title=""><b><%= chat.receiver.usuario %></b></cite></small>
        <%= render(partial: 'links', locals: {chat: chat}) if current_user.admin? %>
      </blockquote>
    <% elsif chat.receiver == current_user %>
      <blockquote class="blockquote-reverse">
        <p><%= chat.mensaje %></p>
        <small>Enviado por <cite title=""><b><%= chat.sender.usuario %></b></cite></small>
        <%= render(partial: 'links', locals: {chat: chat}) if current_user.admin? %>
      </blockquote>
    <% end %>
  <%end%>
</div>
<%= link_to 'New Chat', new_user_chat_path(current_user), class: 'btn btn-primary' %>