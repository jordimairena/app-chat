<div class="well">
  <legend>Iniciar Sesion</legend>
  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <%= f.input :usuario %>
    <%= f.input :password %>
    <div class="row">
      <div class="col col-md-12">
        <%= f.submit "Iniciar Sesion", class:'col-lg-offset-2 col-md-8  btn btn-primary' %>
      </div>
      <div class="col col-md-12 tm-10">
        <%= link_to "Registrarse", new_registration_path(resource_name), class: 'btn btn-default text-center col-lg-offset-2 col col-md-8' %>
      </div>
    </div>
  <% end %>
</div>